"""Reflection agents — weekly reflection + memory-driven proactive outreach.

These complement the sensing package.  Sensing writes raw signals;
reflection *interprets* them (weekly) and triggers action (proactive).
"""
